"""Real focuser dialects, hardware-facing.

Host implementations exist so a dialect can be exercised from the Mac over a
USB-serial link before it is written in C++. Learning a protocol is much faster
in Python than on the ESP32.
"""
